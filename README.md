# Vagrant VM provisioning with ansible

# Prerequisites
- Virtualbox installed 
- Vagrant installed

# Instructions

```
# install ansible and vagrant locally
brew install ansible

# start machine provisioning
vagrant up --provision

# ssh into machine vagrant ssh
vagrant ssh
```